# Imagem para execução dos testes (node_modules no filesystem do container,
# evitando o I/O lento do bind-mount — mesmo fluxo do CI).
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
CMD ["npm", "test"]
